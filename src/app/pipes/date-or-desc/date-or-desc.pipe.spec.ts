import { DateOrDescPipe } from './date-or-desc.pipe';

describe('DateOrDescPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOrDescPipe();
    expect(pipe).toBeTruthy();
  });
});
